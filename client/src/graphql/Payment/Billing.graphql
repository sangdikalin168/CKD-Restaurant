query Billing($tableId: Float!) {
  Billing(table_id: $tableId) {
    payment_id
    items {
      detail_id
      order_id
      product_id
      product_name
      quantity
      unit_price
      description
    }
  }
}
